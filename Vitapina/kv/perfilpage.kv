<PerfilPage>
    FloatLayout:
        canvas:
            Color:
                rgb: utils.get_color_from_hex("#FFFBF0")
            Rectangle:
                size: self.size
                pos: self.pos

        Label:
            canvas.before:
                Color:
                    rgb: utils.get_color_from_hex("#139bad")
                RoundedRectangle:
                    pos: self.pos
                    size: self.size
                    radius: [(0, 0), (0, 0), (20, 20), (20, 20)]
            text: ""
            pos_hint: {"right": 1, "top": 1}
            size_hint: 1, 0.38

        Label:
            id: label_nome
            text: "[size=25][b]Nome Sobrenome[/b][/size]"
            pos_hint: {"right": 0.75, "top": 0.98}
            size_hint: 0.1, 0.38
            markup: True
            halign: 'left'

        Label:
            text: "Usuário há XX dias"
            pos_hint: {"right": 0.75, "top": 0.95}
            size_hint: 0.1, 0.38
            markup: True
            id: label_data
            halign: 'left'

        LabelButton:
            canvas.before:
                Color:
                    rgb: utils.get_color_from_hex("#1f4599")
                RoundedRectangle:
                    pos: self.pos
                    size: self.size
                    radius: [(20, 20), (20, 20), (20, 20), (20, 20)]

            pos_hint: {"right": 0.82, "top": 0.72}
            size_hint: 0.25, 0.05
            text: "[color=#FFFFFF]Editar Perfil[/color]"
            id: label_total_vendas
            markup: True
            on_release: app.alterar_campos()

        Image:
            pos_hint: {"right": 0.625, "top": 1}
            size_hint: 0.25, 0.1
            source: "icones/logotipo.png"
            id: foto_perfil

        Image:
            pos_hint: {"right": 0.4, "top": 0.9}
            size_hint: 0.3, 0.25
            source: "icones/fotos_perfil/foto3.png"
            id: foto_perfil

# ------------------------------------------------------------------------------

        GridLayout: #NOME E SOBRENOME
            pos_hint: {"right": 0.85, "top": 0.55}
            size_hint: 0.7, 0.055
            cols: 2
            size_hint_y: None
            spacing: 2, 2
            height: self.minimum_height
            row_default_height: "30dp"
            row_force_default: True
            halign: 'left'
            spacing: 25, 0

            GridLayout: #NOME
                cols: 1
                size_hint_y: None
                spacing: 2, 2
                height: self.minimum_height
                row_default_height: "30dp"
                row_force_default: True
                halign: 'left'
                Label:
                    text: "[color=#000000][size=17][b]Nome:[/b][/size][/color]"
                    markup: True
                    halign: 'left'
                    size_hint_x: None  # Remove o comportamento de dimensionamento automático
                    width: 65

                TextInput:
                    disabled: True
                    background_disabled_normal: ''
                    background_normal: ''  # Remove o fundo
                    disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                    disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                    foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                    cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                    id: nome
                    hint_text: "Nome"

                    canvas.before:
                        Color:
                            rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                        RoundedRectangle:
                            pos: self.pos
                            size: self.size
                            radius: [15]  # Cantos arredondados

                        Color:
                            rgb: utils.get_color_from_hex("#000000")
                        Line:
                            width: 0.8  # Espessura da borda
                            rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas


            GridLayout: # SOBRENOME
                cols: 1
                size_hint_y: None
                spacing: 2, 2
                height: self.minimum_height
                row_default_height: "30dp"
                row_force_default: True
                halign: 'left'
                Label:
                    text: "[color=#000000][size=17][b]Sobrenome:[/b][/size][/color]"
                    markup: True
                    halign: 'left'
                    size_hint_x: None  # Remove o comportamento de dimensionamento automático

                TextInput:
                    disabled: True
                    background_normal: ''  # Remove o fundo
                    background_disabled_normal: ''
                    foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                    cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                    id: sobrenome
                    hint_text: "Sobrenome"

                    canvas.before:
                        Color:
                            # Verifica se está desabilitado para mudar a cor de fundo
                            rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                        RoundedRectangle:
                            pos: self.pos
                            size: self.size
                            radius: [15]  # Cantos arredondados

                        Color:
                            rgba: (0, 0, 0, 1)  # Cor da borda
                        Line:
                            width: 0.8  # Espessura da borda
                            rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas

    # ------------------------------------------------------------------------------
        GridLayout: # TELEFONE
            pos_hint: {"right": 0.85, "top": 0.495}
            size_hint: 0.7, 0.055
            cols: 1
            size_hint_y: None
            spacing: 2, 2
            height: self.minimum_height
            row_default_height: "30dp"
            row_force_default: True
            halign: 'left'
            Label:
                text: "[color=#000000][size=17][b]Telefone:[/b][/size][/color]"
                markup: True
                halign: 'left'
                size_hint_x: None  # Remove o comportamento de dimensionamento automático
                width: 85  # Define largura fixa

            TextInput:
                disabled: True
                background_disabled_normal: ''
                background_normal: ''  # Remove o fundo
                disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                id: telefone
                hint_text: "Telefone"

                canvas.before:
                    Color:
                        rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                    RoundedRectangle:
                        pos: self.pos
                        size: self.size
                        radius: [15]  # Cantos arredondados

                    Color:
                        rgb: utils.get_color_from_hex("#000000")
                    Line:
                        width: 0.8  # Espessura da borda
                        rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas
    # ---------------------------------------------------------------
        GridLayout:  # EMAIL
            pos_hint: {"right": 0.85, "top": 0.390}
            size_hint: 0.7, 0.055
            cols: 1
            size_hint_y: None
            spacing: 2, 2
            height: self.minimum_height
            row_default_height: "30dp"
            row_force_default: True
            halign: 'left'
            Label:
                text: "[color=#000000][size=17][b]E-mail:[/b][/size][/color]"
                markup: True
                halign: 'left'
                size_hint_x: None  # Remove o comportamento de dimensionamento automático
                width: 70  # Define largura fixa

            TextInput:
                disabled: True
                background_disabled_normal: ''
                background_normal: ''  # Remove o fundo
                disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                id: email
                hint_text: "E-mail"

                canvas.before:
                    Color:
                        rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                    RoundedRectangle:
                        pos: self.pos
                        size: self.size
                        radius: [15]  # Cantos arredondados

                    Color:
                        rgb: utils.get_color_from_hex("#000000")
                    Line:
                        width: 0.8  # Espessura da borda
                        rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas

#-------------------------------------------------------------------
        GridLayout:
            pos_hint: {"right": 0.85, "top": 0.230}
            size_hint: 0.7, 0.055
            cols: 2
            size_hint_y: None
            spacing: 90, 0
            height: self.minimum_height
            row_default_height: "30dp"
            row_force_default: True
            halign: 'left'

            GridLayout:  #DATA DE NASCIMENTO
                pos_hint: {"right": 0.59, "top": 0.240}
                size_hint: 0.40, 0.055
                cols: 1
                size_hint_y: None
                spacing: 2, 2
                height: self.minimum_height
                row_default_height: "30dp"
                row_force_default: True
                halign: 'left'

                Label:
                    text: "[color=#000000][size=17][b]Data de Nascimento:[/b][/size][/color]"
                    markup: True
                    halign: 'left'
                    size_hint_x: None  # Remove o comportamento de dimensionamento automático
                    width: 170  # Define largura fixa

                GridLayout:
                    cols: 3
                    size_hint_y: None
                    spacing: 2, 2
                    height: self.minimum_height
                    row_default_height: "30dp"
                    row_force_default: True
                    halign: 'left'

                    TextInput: # DIA
                        disabled: True
                        background_disabled_normal: ''
                        background_normal: ''  # Remove o fundo
                        disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                        disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                        foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                        cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                        id: dia_nas
                        hint_text: "DD"
                        size_hint: 0.065, 0.055
                        halign: "center"

                        canvas.before:
                            Color:
                                rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                            RoundedRectangle:
                                pos: self.pos
                                size: self.size
                                radius: [15]  # Cantos arredondados

                            Color:
                                rgb: utils.get_color_from_hex("#000000")
                            Line:
                                width: 0.8  # Espessura da borda
                                rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas

                    TextInput: # MES
                        disabled: True
                        background_disabled_normal: ''
                        background_normal: ''  # Remove o fundo
                        disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                        disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                        foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                        cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                        id: mes_nas
                        hint_text: "MM"
                        size_hint: 0.065, 0.055
                        halign: "center"

                        canvas.before:
                            Color:
                                rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                            RoundedRectangle:
                                pos: self.pos
                                size: self.size
                                radius: [15]  # Cantos arredondados

                            Color:
                                rgb: utils.get_color_from_hex("#000000")
                            Line:
                                width: 0.8  # Espessura da borda
                                rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas

                    TextInput: # ANO
                        disabled: True
                        background_disabled_normal: ''
                        background_normal: ''  # Remove o fundo
                        disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                        disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                        foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                        cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                        id: ano_nas
                        hint_text: "AAAA"
                        size_hint: 0.1, 0.055
                        halign: "center"

                        canvas.before:
                            Color:
                                rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                            RoundedRectangle:
                                pos: self.pos
                                size: self.size
                                radius: [15]  # Cantos arredondados

                            Color:
                                rgb: utils.get_color_from_hex("#000000")
                            Line:
                                width: 0.8  # Espessura da borda
                                rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas


            GridLayout:
                pos_hint: {"right": 0.85, "top": 0.240}
                size_hint: 0.1, 0.055
                cols: 1
                size_hint_y: None
                spacing: 2, 2
                height: self.minimum_height
                row_default_height: "30dp"
                row_force_default: True
                halign: 'left'

                Label:
                    text: "[color=#000000][size=17][b]Sexo:[/b][/size][/color]"
                    markup: True
                    halign: 'left'
                    size_hint_x: None  # Remove o comportamento de dimensionamento automático
                    width: 45 # Define largura fixa

                TextInput: # SEXO
                    disabled: True
                    background_disabled_normal: ''
                    background_normal: ''  # Remove o fundo
                    disabled_foreground_color: 0, 0, 0, 1  # Cor do texto (preto) quando desabilitado
                    disabled_background_color: 1, 1, 1, 0  # Fundo transparente quando desabilitado
                    foreground_color: 0, 0, 0, 1  # Cor do texto (preto)
                    cursor_color: 0, 0, 0, 1  # Cor do cursor (preto)
                    id: sexo
                    hint_text: "-"
                    halign: "center"


                    canvas.before:
                        Color:
                            rgba: (0.996, 0.933, 0.745, 1) if self.disabled else utils.get_color_from_hex("#fffbf0")
                        RoundedRectangle:
                            pos: self.pos
                            size: self.size
                            radius: [15]  # Cantos arredondados

                        Color:
                            rgb: utils.get_color_from_hex("#000000")
                        Line:
                            width: 0.8  # Espessura da borda
                            rounded_rectangle: [self.x, self.y, self.width, self.height, 15]  # Bordas arredondadas

        LabelButton:
            pos_hint: {"right": 0.53, "top": 0.180}
            size_hint: 0.1, 0.1
            text: "[color=#0000FF][u]Sair[/u][/color]"
            id: label_total_vendas
            markup: True
            on_release: app.mudar_tela("loginpage")
        GridLayout:
            rows: 1
            pos_hint: {"right": 1, "top": 0.07}
            size_hint: 1, 0.07
            canvas:
                Color:
                    rgb: utils.get_color_from_hex("#8dd0d9")
                Rectangle:
                    size: self.size
                    pos: self.pos
            ImageButton:
                source: "icones/receita.png"
                on_release: app.mudar_tela("receitaspage")
            ImageButton:
                source: "icones/fogo.png"
                on_release: app.mudar_tela("caloriaspage")
            ImageButton:
                source: "icones/graph.png"
                on_release: app.mudar_tela("glicemiapage")
            ImageButton:
                source: "icones/user.png"
                on_release: app.mudar_tela("perfilpage")